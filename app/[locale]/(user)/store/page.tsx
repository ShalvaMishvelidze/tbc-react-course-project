import StoreHeader from "@/components/StoreHeader";
import React from "react";

const page = () => {
  return (
    <section className="store">
      <StoreHeader />
    </section>
  );
};

export default page;
